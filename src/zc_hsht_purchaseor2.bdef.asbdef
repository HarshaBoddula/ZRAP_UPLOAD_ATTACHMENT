projection;
strict ( 2 );
extensible;
use draft;
use side effects;

define behavior for ZC_HSHT_PURCHASEOR2 //alias <alias_name>
extensible
use etag
{
  use create;
  use update;
  use delete;

  use action UploadAttachment;

  use action Activate;
  use action Discard;
  use action Edit;
  use action Resume;
  use action Prepare;
}