projection implementation in class ZBP_C_HSHT_PURCHASEOR unique;
strict ( 2 );
extensible;
use draft;
use side effects;
define behavior for ZC_HSHT_PURCHASEOR alias ZcHshtPurchaseor
extensible
use etag
{
  use create;
  use update;
  use delete;

  use action UploadAttachment;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;

}